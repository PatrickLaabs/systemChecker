<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>VM Status</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
      margin-top: 20px;
    }

    th,
    td {
      text-align: left;
      padding: 8px;
    }

    th {
      background-color: #4CAF50;
      color: white;
    }

    tr.Up {
      background-color: #8BC34A;
    }

    tr.Down {
      background-color: #FF5252;
    }
  </style>
</head>

<body>
  <h1>VM Status</h1>
  <table>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Status</th>
    </tr>
    {{range $vm := .vms}}
    <tr class="{{if $vm.Status}}Up{{else}}Down{{end}}">
      <td>{{$vm.Name}}</td>
      <td>{{$vm.Address}}</td>
      <td>{{if $vm.Status}}Up{{else}}Down{{end}}</td>
    </tr>
    {{end}}
  </table>
</body>

</html>
